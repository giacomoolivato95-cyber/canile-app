name: canile_app
description: App per la gestione del canile
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  supabase_flutter: ^2.5.0
  intl: ^0.19.0
  table_calendar: ^3.0.0
  provider: ^6.1.0
  uuid: ^4.0.0
  hive_flutter: ^1.1.0
  hive: ^2.2.3
  connectivity_plus: ^5.0.0
  path_provider: ^2.1.0
  firebase_core: ^4.12.1
  firebase_messaging: ^16.4.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  hive_generator: ^2.0.0
  build_runner: ^2.4.0

flutter:
  uses-material-design: true